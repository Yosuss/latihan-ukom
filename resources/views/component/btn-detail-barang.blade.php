<form action="{{ route('detailBarang', $item->id_barang) }}">
    <button id="detail"
        class="bg-yellow-500 hover:bg-yellow-700 text-white px-4 py-1 rounded-lg font-semibold w-full">
        {{-- detail --}}
        detail
    </button>
</form>
