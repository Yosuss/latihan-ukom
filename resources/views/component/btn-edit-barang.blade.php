<form action="{{ route('editBarang', $item->id_barang) }}">
    <button id="edit"
        class="bg-blue-500 hover:bg-blue-700 text-white px-4 py-1 rounded-lg font-semibold w-full">
        {{-- edit --}}
        edit
    </button>
</form>
